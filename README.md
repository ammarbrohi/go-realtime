# GoRealTime - Real-time Chat in Go using GraphQL

## Running

Run server:

```
$ git clone https://github.com/ammarbrohi/go-realtime.git
$ cd go-realtime
$ docker-compose up -d --build
```

Run Vue app:

```
$ cd frontend
$ npm run start
```
